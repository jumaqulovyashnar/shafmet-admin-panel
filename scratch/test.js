import axios from 'axios';

async function test() {
  try {
    // Let's read the .env file to get the token or just fetch public info,
    // wait we can read userStore from sessionStorage or just run a query.
    console.log("Fetching workers...");
  } catch (err) {
    console.error(err);
  }
}
test();
